{{#if column.hideLabel}}
  &nbsp;
{{else}}
	{{#if (eq column.key 'select_all')}}
		{{checkbox-component selected=filter onChange=(action 'selectAll') class="filter_select_all"}}
	{{/if}}
	{{if column.header column.header ''}}
{{/if}}
